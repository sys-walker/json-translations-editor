{
  "name": "TranslationEditorManager",
  "version": "0.0.1",
  "author": "Pere Antoni Rollon",
  "homepage": "https://github.com/sys-walker",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pretty": "npx prettier --check ./docs/",
    "pretty-check": "npx prettier --check ./docs/",
    "pretty-write": "npx prettier --write ./docs/"
  },
  "license": "ISC",
  "private": true,
  "description": "A Prettier codestyle",
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
