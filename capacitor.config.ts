import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'json-translations-editor-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
